@AGENTS.md

Project instructions live in `AGENTS.md`.
Current open work lives in `Docs/Implementation Roadmap.md`.
Current architecture lives in `Docs/Implementation Notes.md`.

## Agent skills

### Issue tracker

Issues live in GitHub Issues (`luminagency-cloud/dealer-intel`), via `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
